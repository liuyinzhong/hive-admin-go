# Hive Admin Go 后端开发规范

## 项目范围

`hive-admin-go` 是 Hive 系统后端服务，当前技术栈以 `go.mod` 和现有代码为准：

- Go `1.25.0`
- Gin
- GORM + MySQL
- JWT Bearer Token
- Swagger（已启用）

主要目录：

- `router`：路由注册和分组。
- `controllers`：HTTP 参数绑定、校验和响应。
- `services`：业务逻辑和数据库操作。
- `models`：数据库模型、请求/响应结构。
- `database`：数据库初始化和连接。
- `middleware`：认证等中间件。
- `utils`：项目公共工具。
- `docs`：Swagger 自动生成文件。

## 开发前

进入任何已有业务模块时，必须先阅读该模块在 `business-docs` 中的领域词汇、业务规则和前端 UI 说明，用文档先建立对业务目标、状态、边界和运行逻辑的整体理解，再阅读代码核实现状和定位实现入口。不得一开始通过逐文件钻研代码来反推业务规则；代码用于验证文档、发现差异和完成实现，不代替文档提供业务上下文。

按以下顺序获取上下文：

1. 业务功能先阅读 `business-docs/README.md`、`CONTEXT-MAP.md` 和对应领域手册。
2. 阅读当前模块的 Router、Controller、Service 和 Model。
3. 查找相似接口、分页、事务、导入导出和数据库操作。
4. 阅读 Swagger 注释和相关 SQL。
5. 核对前端调用方式和字段类型。
6. 确认没有成熟实现后再设计新方案。

项目代码是重要参考，但不要机械复制旧问题。现有代码、文档和规则不一致时，先说明差异。

### 业务文档一致性规则

`business-docs` 是业务人员、开发、测试和 AI 共用的业务知识库。新增功能、修改既有功能或修复会改变外部可观察行为的缺陷时，文档同步属于同一项工作的完成条件，不能把文档更新留作未落实的后续事项。

#### 文档边界

- 后端 `business-docs/<领域>` 是术语、业务规则、状态流转、前置条件、副作用、权限和代码入口的正文。
- 前端 `../hive/business-docs/<领域>` 只记录页面入口、按钮展示、表单联动、刷新和交互规则，不复制另一套后端业务规则。
- `business-docs/README.md` 是 Controller、Service、Model 和路由到业务领域的总索引；新增业务入口时必须登记。
- `CONTEXT.md` 只定义领域词汇，不写接口、表名、权限码或实现细节；具体规则放入模块文档。
- `CONTEXT-MAP.md` 维护领域入口和跨领域关系。增加新领域或改变跨模块依赖时必须同步更新。
- 本仓库 `docs` 是 Swagger 生成文件，不存放手写业务文档；Swagger 只描述 HTTP 契约，不能代替业务规则。

#### 新增功能时

1. 先判断应归入现有领域还是建立新领域，避免为单个页面随意创建重复上下文。
2. 新领域在后端建立 `business-docs/<领域>/CONTEXT.md`、`README.md` 和按模块拆分的规则文档，并在 `CONTEXT-MAP.md` 注册。
3. 有前端页面时，在前端建立对应 `business-docs/<领域>/README.md` 和 `*-ui.md`，链接到后端规则正文。
4. 为新模块源码目录增加或更新就近 `AGENTS.md`，明确 AI 的文档阅读顺序。
5. 使用稳定且唯一的规则编号；已有编号不得重新分配给不同含义。

#### 修改功能时

必须逐项判断并同步受影响文档：

- 术语、模块边界和上下文关系；
- 状态、状态流转、前置条件、业务校验和终态；
- 数据写入、库存、价格、号源、消息、文件、日志等副作用；
- 接口地址、方法、字段、类型、必填、枚举、错误语义和分页限制；
- 权限码、菜单按钮和角色可执行动作；
- 页面入口、按钮条件、表单联动、刷新策略和失败表现；
- 定时任务、异步任务、幂等、并发、保留期和清理规则；
- 与其它领域、前端或外部系统的关系。

纯重构、格式化或内部性能调整如果确认没有改变业务语义，可以不改业务正文，但交付前仍必须完成文档影响检查，并在交付说明中明确“业务文档已核对，无语义变化”。

#### 不一致处理与验证

- 文档、代码、Swagger、SQL 和前端行为不一致时，先列出差异、当前实际行为和影响；不得静默按其中任一方覆盖其它内容。
- 用户当前确认的新规则优先于旧文档，但实现时必须在同一次修改中更新旧文档，不能让两套规则长期并存。
- 完成前检查前后端文档链接、规则编号唯一性、UTF-8 编码和 `docs` 目录边界。
- 交付总结必须说明修改了哪些业务文档；若没有修改，必须说明核对结果和不需要修改的原因。
- 业务文档未同步时，不得声称功能完整交付。

### ERP 业务文档

处理 `erp_*` Router、Controller、Service、Model/DTO、迁移或 Swagger 前，必须按以下顺序阅读：

1. `CONTEXT-MAP.md`。
2. `business-docs/erp/CONTEXT.md`。
3. `business-docs/erp/README.md`。
4. `business-docs/erp` 下当前子模块规则。
5. 当前 Router、Controller、Service、Model/DTO，以及前端对应 API 和页面。

`business-docs/erp` 是 ERP 新任务的业务文档入口；根目录 `CONTEXT.md` 中仍有待逐步迁移的历史 ERP 内容。代码、Swagger 与业务文档不一致时，必须列出差异和影响，不得静默选择。ERP 术语、状态、业务前置条件、库存副作用或权限变化时，在同一次修改中同步对应文档；不要把实现细节继续追加到领域词汇文件。

### 产品与医疗业务文档

处理 `product_*` 前，按 `CONTEXT-MAP.md` → `business-docs/product/CONTEXT.md` → `business-docs/product/README.md` → 当前子模块规则 → 当前代码与前端实现的顺序阅读。

处理任一医疗模块前，按 `CONTEXT-MAP.md` → `business-docs/medical/CONTEXT.md` → `business-docs/medical/README.md` → 当前模块规则 → 前端对应 UI 文档 → 当前代码的顺序阅读。

产品档案，以及科室、医生、患者、诊断、挂号费、排班、挂号候诊、接诊和处方审核的术语、状态、前置条件、价格或号源副作用、权限及前端入口变化时，必须在同一次修改中同步对应业务文档。不要把新的实现细节追加到领域词汇文件。

### 系统管理业务文档

处理 auth、user、role、dept、menu、permission、dict、param、file、audit、external_page、pay_channel、menu_message、download_task、导出器或相关路由前，按 `CONTEXT-MAP.md` → `business-docs/system/CONTEXT.md` → `business-docs/system/README.md` → 当前模块规则 → 前端对应 UI 文档 → 当前代码的顺序阅读。

系统管理各子模块不能互相套用规则。修改登录授权、动态菜单、字典参数、敏感配置、审计、消息持久化、事件类型、任务状态、导出上限、文件保留、清理规则、来源导出器或权限时，必须在同一次修改中同步系统业务文档；涉及其它来源领域时还要同步其文档。

## 技能自动调用

本仓库技能位于 `.agents/skills`，遵循工作区根 `AGENTS.md` 的通用技能路由。任务与技能说明匹配时，无需用户点名，必须先阅读对应 `SKILL.md` 再使用。

- 新增或修改 Router、Controller、Service、Model/DTO 时，使用 `implement`；默认不因小型功能或缺陷修复主动新增 `_test.go` 等测试文件。只有用户明确要求、已有测试文件需要同步维护，或改动高风险且已先说明必要性时，才组合使用 `tdd` 并新增或修改测试文件。
- 接口异常、事务问题、GORM 查询错误、性能回退或偶发失败的根因不明时，使用 `diagnosing-bugs`，先建立不启动服务、不连接生产环境的最小反馈命令。
- 设计分层、事务边界、查询接口、公共 Service 或适配器时，使用 `codebase-design`，并继续遵守 Router -> Controller -> Service/Model 的依赖方向。
- 前后端字段、DTO、数据库模型、枚举或业务术语存在歧义时，使用 `domain-modeling`；需要集中澄清并沉淀文档时组合 `grilling` 与 `grill-with-docs`。
- 只有纯业务状态或算法问题需要可运行实验时才使用 `prototype`；原型不得连接生产数据库、执行迁移或触发 Apifox 同步。
- 完成有实质代码变更的实现后使用 `code-review`，重点核对分层、统一响应、参数校验、事务、权限、SQL 安全、Swagger 和前后端契约。
- 只有用户明确要求规格、任务拆分或架构巡检时，才使用 `to-spec`、`to-tickets`、`improve-codebase-architecture`；不得自动向外部工单系统发布内容。
- 技能不能作为修改认证授权、数据库结构、公共响应、配置加载、依赖或生成文档流程的默认授权。

## 分层与依赖

允许的主要依赖方向：

```text
Router -> Controller
Controller -> Service、Model
Service -> Database、Model、Utils
Middleware -> 认证及基础设施
```

- Router 只负责路由、分组和中间件注册。
- Controller 只负责参数绑定、参数校验、调用 Service 和返回统一响应。
- Service 负责完整业务、数据库操作、事务、数据组装和转换。
- Model 定义数据结构和映射，不访问 Controller、Service 或 Database。
- Service 不返回 Gin HTTP Response，Controller 不直接访问数据库或拼装 SQL。
- 不跨层调用，不在 Controller 中拆散同一业务事务。

## Router 与接口

- 保持现有 `/api/auth`、`/api/system`、`/api/dev`、`/api/workflow` 等模块划分。
- 普通资源接口保持 RESTful 风格，资源名称使用复数；多词 Path 段使用小驼峰，禁止使用连字符，例如 `/scheduleTemplates`，不得写成 `/schedule-templates`。
- 不跨模块注册路由，不修改无关路由。
- 登录认证是独立的全局公共校验，必须先于具体接口权限校验执行。
- 所有业务模块（包括已有权限码较完整的医疗模块）都必须逐条审查接口权限。新增或修改业务接口默认配置接口权限；只有明确认定为公共接口时才不注册权限中间件，不能把遗漏注册当作公共设计。
- 每条需要接口授权的路由必须显式注册权限中间件，并在路由代码中硬编码一个独占的原子权限码，例如 `permissionGuard.Require("dev:task:update")`；不得从请求参数、菜单标题或前端传值动态决定路由所需权限。
- 同一个接口权限码不得被多条受保护路由共用。新增、修改或删除受保护路由时，必须同步梳理对应权限码和菜单按钮数据，并在交付说明中列出需要手动配置的菜单按钮权限。
- 公共接口不注册权限中间件，即不进入接口权限校验；公共数据源、下拉选项、上传等是否公开必须结合业务风险逐条确认，不能仅凭接口类型自动豁免。
- Path、Query、Body、Header 的命名和类型必须与前端及文档一致。

## Controller 与响应

- 使用项目现有参数绑定和校验方式。
- 统一返回 `models.NewSuccessResponse()` 和 `models.NewErrorResponse()`。
- 保持 `code`、`data`、`error`、`message` 四个字段，不手写另一套 JSON 结构。
- 参数错误、未认证、无权限、未找到、冲突和内部错误应使用稳定、合适的 HTTP 状态码。
- HTTP 状态码只表达 HTTP 层结果，不替代业务 `code` 和 `message`。常用约定：请求成功使用 `200`；参数绑定、格式、必填和枚举错误使用 `400`；未登录或 Token 无效使用 `401`；已登录但缺少接口权限使用 `403`；路由不存在才使用 `404`；业务数据不存在原则上返回业务错误信息，不把它伪装成接口不存在，除非该模块已明确约定资源型详情接口使用 `404`；唯一性、并发版本、状态冲突等业务冲突使用 `409`；未预期的服务端错误使用 `500`。
- Controller 选择 HTTP 状态码时必须保持接口语义稳定，同类错误在同一模块内不得混用不同状态码；Swagger 注释必须与实际返回状态码一致。
- 不把 SQL、文件路径、堆栈或其他内部实现细节直接返回给客户端。
- 内部错误记录必要上下文，对外返回稳定且符合项目风格的提示。

## Service

- 一个 Service 方法完成一个边界清晰的业务操作。
- CRUD、数据组装、批量处理、导入导出和数据权限逻辑统一放在 Service。
- 优先复用已有 Service、公共查询和转换函数。
- 避免超长函数；拆分时按业务职责拆分，不制造只有一行转发的无意义层次。
- 业务错误应保留可判断的类型或语义，不依赖字符串匹配判断错误类别。

## Model 与字段

- 数据库 Model、请求 DTO、响应 DTO 按项目现有方式放入 `models`。
- Go 字段、JSON Tag、数据库字段和前端字段保持一致。
- 接口响应字段统一使用小驼峰 JSON 命名。
- 对用途不同、校验规则不同的结构使用明确 DTO，不直接暴露不应返回的数据库字段。
- 避免重复结构体；局部临时结构确有必要时保持范围最小。

## 数据库与 GORM

- 数据库使用 MySQL，ORM 统一使用 GORM。
- 主键沿用带横线的 UUID，新增数据使用项目已有 UUID 生成方式。
- UUID Path、Query 和 Body 参数必须按现有方式校验，不能把非法值直接传入查询。
- 优先使用 `Where`、`Scopes`、`Select`、`Order`、`Limit`、`Joins` 和 `Preload` 等现有写法。
- 允许使用 GORM 占位符参数化条件，例如 `Where("user_id = ?", userID)`。
- 禁止使用 `fmt.Sprintf` 或字符串拼接把用户输入写入 SQL。
- 动态排序字段必须经过白名单映射，排序方向只允许 `asc`、`desc`，不得直接拼接请求参数。
- 检查所有数据库调用返回的 `Error`，不得忽略关键查询或写入错误。

## 业务编码

- 业务可读编码与数据库主键分离：主键继续使用 UUID，企业编码、SPU 编码、RP 编码、SKU 编码等业务编码统一走公共编码流水。
- 公共编码流水表为 `base_code_sequence`，以 `sequence_type` 区分编码域；常见取值示例：`ENTERPRISE`、`PRODUCT_SPU`、`PRODUCT_RP`、`PRODUCT_SKU`。
- 新增需要自动生成业务编码的模块时，必须复用公共编码流水调用方式，不得为单个模块新增独立流水表、独立自增字段或前端生成编码。
- 生成编码必须放在对应业务写入事务中执行，并始终使用传入的 `tx`；读取流水行时必须使用行级锁，避免并发下重复编码。
- 编码格式由 `base_code_sequence.prefix`、`current_value`、`number_length` 共同决定；若某类编码不需要前缀，允许将 `prefix` 配置为空字符串，但仍复用同一套流水机制。
- 固定编码域建议在正式迁移 SQL 中初始化对应 `sequence_type`、`prefix` 和 `number_length`；未初始化时，Service 可使用明确的默认值兜底创建，但不得静默使用含义不明的编码域。
- 新模块不要复制粘贴企业主体私有方法；如多个 Service 需要生成编码，应先抽取为公共 Service 或工具函数，再由业务 Service 在事务内调用。

## 查询与性能

- 分页参数统一使用 `page`、`pageSize`，排序参数使用 `sorts`，多选参数沿用逗号分隔格式。
- 分页默认值、最小值和最大值沿用现有实现；新增入口不得允许无上限的大页查询。
- 列表接口避免 N+1、循环查询和重复查询，优先批量查询、关联查询或合理的 `Preload`。
- 只查询需要的字段，大数据列表必须分页。
- 新增复杂或高频查询时检查索引使用情况，必要时使用 `EXPLAIN`，但不直接操作生产库。

## 事务

以下场景应使用事务：

- 多条关联写操作必须整体成功或失败。
- 多表写入或批量写入要求原子性。
- 读后写过程要求并发一致性。
- 任一步失败都不能保留部分结果。

单条原子 `INSERT`、`UPDATE`、`DELETE` 不强制开启显式事务。

事务中必须始终使用传入的 `tx`，不得混用全局 `database.DB`。所有错误路径都要正确回滚；外部网络调用和耗时文件操作原则上不要放在数据库事务内。

## 数据库结构变更

- 数据库设计有疑问时先询问，不自行增加重复字段或改变已有字段语义。
- 已执行或已发布的 SQL 脚本不直接修改，应新增迁移脚本。
- 表、字段、类型、索引和批量数据变更必须说明兼容性、数据风险及回滚方案。
- 未经明确授权不执行迁移、不批量修改数据、不使用自动迁移代替正式迁移脚本。
- 前后端类型、Model、查询和 Swagger 必须与数据库变更同步。
- 禁止自动在数据库中创建备份库、备份表、影子表或复制表，包括但不限于 `*_backup_*`、`*_bak_*`、`CREATE DATABASE`、`CREATE TABLE ... AS SELECT`、`CREATE TABLE ... LIKE` 后复制数据等形式。
- 即使涉及迁移、批量修复、菜单权限调整、排查或回滚准备，也不得代替用户创建数据库备份对象。
- 如确需备份，只能提示用户手动创建备份，并在用户明确确认备份已完成后继续后续工作。

## 临时文件

- 运行任务、验证方案或排查问题时产生的一般临时文件，统一放到仓库根目录的 `temp/` 目录下；目录不存在时先创建。
- 临时 SQL 查询、分析、验证、迁移草稿、回滚草稿、数据修复草稿、测试 SQL、导入导出样例、执行输出和一次性分析结果，统一放到仓库根目录的 `sql_cache/` 目录下；目录不存在时先创建，不得放入 `temp/`、仓库根目录、业务源码目录、`business-docs/` 或正式 `migrations/` 目录。
- `sql_cache/` 是临时文档缓存目录，不属于正式项目源码；其中内容不得自动生成 Swagger、业务文档或正式迁移记录，也不得默认提交到 Git。
- 只有用户确认需要长期维护的正式源码、正式测试用例或正式迁移脚本，才放入对应业务目录；正式迁移脚本放入 `migrations/`，并在交付说明中标明用途、兼容性和回滚方式。
- 已有 SQL 文件不因本规则被移动、删除或改名；本规则只约束今后生成临时 SQL 文件的位置。

## 时间

- 当前业务接口默认时间格式为 Go layout：`2006-01-02 15:04:05`。
- 解析本地业务时间时使用 `time.ParseInLocation` 和项目统一的 `*time.Location`。
- 不依赖部署机器隐式时区新增时间逻辑；存储、比较和输出必须使用一致时区。
- 外部协议明确要求 UTC 或 RFC3339 时，以接口契约为准并在边界处转换。
- 不在不同模块重复实现时间解析和格式化函数。

## 认证与授权

- 认证统一使用 `Authorization: Bearer <token>` 和现有 JWT 中间件。
- 不新增 Cookie 登录、自定义认证 Header 或 Controller 内 Token 解析。
- 认证只表示用户身份有效，不能代替具体接口授权；接口权限是粗粒度 HTTP 门禁，不等同于数据权限。
- `is_sys = 1` 的系统用户在通过登录认证后直接通过接口权限校验，不按用户名、角色显示名称或 `superAdmin` 字符串判断管理员。
- 普通用户的有效权限是该用户关联的所有角色所关联权限码的并集。读取 `sys_menu.auth_code` 时按英文逗号拆分，逐项 Trim、忽略空值并去重，再与路由所需原子权限码精确匹配；不得使用前缀、包含、通配符或业务语义推断。
- 权限汇总必须与 `/api/auth/codes` 复用同一 Service 或底层逻辑，禁止分别实现两套角色权限计算规则。
- 权限校验中间件只负责两种结果：拥有所需权限时放行，不拥有时返回 HTTP 403。中间件不负责日志管理、错误语义化、数据范围、字段脱敏或接口内部业务动作授权。
- 只校验用户直接触发的 HTTP 路由。接口内部调用其他 Service 或执行其他业务动作时，不递归校验那些动作对应的接口权限码。
- 新增权限默认不授予任何角色；角色授权必须通过独立、明确的授权操作完成。
- 修改 JWT、登录、认证中间件或权限逻辑前必须说明影响和兼容性。

## 数据权限

数据权限的业务正文和当前全路由分类见 `business-docs/system/data-permission.md`。AI 新增或修改任何 Router、Controller、Service、统计、导出器或带用户归属的 Model 时，必须先阅读该文档并遵守以下规则：

- 每条接口必须明确归为：公开接口、角色数据范围、当前用户归属、来源对象继承、领域归属或全局主数据。不得把“尚未接入过滤”写成全局主数据，也不得因接口是选项、详情、统计或内部调用就跳过分类。
- 角色数据范围统一复用 `datapermission.Permission` 和请求中间件解析结果。Controller 只传递范围，Service 在最终业务查询上应用；不得相信前端提交的用户 ID、部门 ID、范围值或自定义 SQL 条件代替当前权限。
- 同一资源的列表、全量选项、详情、修改、状态动作、删除、批量操作、统计、打印和导出必须使用一致边界。按 ID 写入前必须在同一查询或事务中带范围校验，不能先无范围读取再更新。
- 多归属业务应明确 OR 语义及字段来源。新建数据必须写入可靠的创建人、办理人或其它归属字段；空归属默认仅 `all` 可见，禁止自动当成公共记录。
- 引用受范围控制的用户、文件、版本、需求、采购单、库存余额、患者、排班等对象时，写入前逐项验证引用可见。批量动作任一 ID 不存在或越界时整批失败，不对可见子集静默成功。
- 部门范围使用当前有效用户部门关系。用户管理写操作还必须防止局部管理员覆盖用户的越界部门、分配自己未持有的角色或授予超出自身的角色范围。
- 全局主数据只能在业务上确属全系统共享时使用，仍必须保留原子接口权限和状态校验。领域归属资源继续校验流程参与者、当前排班医生、处方开具/审核关系等，`all` 不能绕过。
- 异步导出和延迟任务必须在可信任务记录中保存发起用户 ID，并在真正查询数据时重新解析当前权限；不得信任请求负载中的用户 ID，也不得把 SQL 条件、部门 ID 集合或 `All=true` 快照长期保存在任务负载中。
- 修改数据权限语义时必须同步角色 DTO/前端类型、迁移及回滚脚本、索引、Swagger、领域文档和全路由分类矩阵。新增路由交付前逐条与 `router/router.go` 对账。
- 数据权限属于安全边界。至少验证多角色并集、全部范围短路、自定义部门、部门后代、仅本人、无范围、未知值关闭访问、空归属、详情/写入 IDOR 和异步重新解析；已有相关测试文件时必须同步维护。

## 权限码与菜单按钮

- 原子权限码统一为 `模块:资源:动作` 三段格式，例如 `dev:task:batchCreate`、`medical:scheduleTemplate:update`。
- 三段均使用小驼峰，禁止连字符、空格、中文和额外层级。前端专用权限码与后端接口权限码使用同一格式；同一原子权限码可以被多个业务入口的菜单按钮复用，但不能让多条受保护路由共用一个权限码。
- `sys_menu.auth_code` 可使用英文逗号保存多个原子权限码。菜单新增、修改时必须拆分、Trim、过滤空值后，逐个校验格式；相同权限码出现在不同入口按钮中不视为冲突，权限集合消费时统一去重。
- 每个受保护接口的原子权限码必须配置为对应业务菜单下的独立 `type=button` 节点，不得放在目录或菜单节点，也不得创建根级按钮。
- 受保护接口按钮节点的 `auth_code` 只保存该接口的独占原子权限码。前端专用权限码也可建立按钮节点，但后端路由无需引用它。
- `type=button` 节点必须满足：`name = NULL`、`title` 为国际化 key、`auth_code` 为权限码，并且不参与动态路由生成。非按钮菜单仍必须有有效、语义明确的路由名称。
- 按钮 `title` 使用点分国际化 key，例如 `system.permission.menuUpdate`、`dev.permission.taskAdvance`；`title` 不得填写权限码，`auth_code` 也不得填写国际化 key。
- 国际化命名空间与实际前端语言包一致。工作流语言包为 `flow.json`，因此可使用权限码 `workflow:task:approve`，但标题应写为 `flow.permission.taskApprove`。
- 按钮文案应简短、准确表达动作，例如“新增、修改、删除、详情、列表、发布”，并由前端同步提供中英文翻译。
- 新增或调整受保护接口、权限码、菜单按钮时，不在业务迁移 SQL 中自动写入或修改 `sys_menu` 菜单/按钮权限数据。必须在交付说明中列出需要手动配置的按钮名称、`auth_code`、`title` 国际化 key、建议父级和注意事项，由用户确认后手动维护。
- 存量权限码和菜单按钮同样受本规范约束，尤其医疗模块不得沿用例外格式。发现连字符、非小驼峰、格式错误或错误节点类型时，不直接写入自动修正 SQL；应列出待修正清单、风险和回滚建议，等待用户决定如何手动维护。

## 文件上传下载

- 复用现有上传目录、文件 Service 和接口，不新增另一套上传逻辑。
- 校验文件名、大小、类型和路径，防止路径穿越和任意文件覆盖。
- 数据库写入与文件落盘要考虑失败清理，避免孤儿记录或孤儿文件。
- 不把本地绝对路径或内部错误直接返回客户端。

## Swagger 文档

本项目已经启用 Swagger。新增或修改接口时必须：

- 同步维护 Controller 中的 Swagger 注释。
- 接口分组统一使用 `@Tags 大模块/子模块` 格式，例如 `@Tags 系统管理/日志管理`、`@Tags 开发管理/模块管理`；Apifox 同步时直接按该格式识别二级目录，不再依赖同步前的转换逻辑。
- 没有子模块的接口可继续使用单层 `@Tags 模块名`，同步 Apifox 时保持单层目录。
- 每个接口必须完整描述入参，包括 Path、Query、Body、Header 中实际使用的参数；参数名称、类型、是否必填、枚举范围和业务含义必须与 Controller 绑定结构、Model/DTO、前端 API 保持一致。
- 请求体必须引用明确的请求 DTO，不得用含义不清的通用结构代替；请求 DTO 字段应通过 JSON Tag、校验 Tag、字段注释或 Swagger 注释表达必填、长度、枚举、默认值和示例值。
- 响应必须引用明确的响应 DTO 或统一响应包装后的数据结构；列表接口必须说明分页响应结构，详情、创建、更新、启停等接口必须说明返回的数据对象或空响应约定。
- Swagger 注释必须包含成功响应和主要失败响应，例如参数错误、未认证、无权限、数据不存在、数据冲突和内部错误；HTTP 状态码、业务 `code`、`message` 风格应与项目统一响应一致。
- Swagger 的 `@Success`、`@Failure` 必须列出实际使用的 HTTP 状态码，并用说明文字区分 HTTP 语义和业务错误语义；不得把业务数据不存在、校验失败等场景写成“接口不存在”。
- 每条新增或修改的接口 Swagger 注释必须在 `@Description` 中明确数据权限情况，并使用统一分类：`公开接口`、`角色数据范围`、`当前用户归属`、`来源对象继承`、`领域归属` 或 `全局主数据`。使用数据权限时还要写明实际过滤/校验依据（如 `creator_id`、办理人、参与人、部门范围、来源业务对象或当前医生）；不使用记录级数据权限时必须写明原因。
- 列表、全量选项、详情、创建、修改、状态动作、删除、批量操作、统计、打印和导出等同一资源接口，Swagger 的数据权限说明必须明确它们采用一致边界；异步导出必须注明按任务执行时的发起人权限重新解析。
- Swagger 数据权限说明必须与 `business-docs/system/data-permission.md`、`router/router.go` 和 Service 实际行为一致，不能仅根据路由中间件名称或前端按钮推断；新增路由交付前逐条核对分类矩阵。
- DTO 字段应提供有业务意义的 `example` 示例值；示例不得使用真实身份证号、手机号、Token、密钥、生产连接串或其他敏感数据。业务编码、UUID、时间、枚举和金额等字段示例必须符合实际格式。
- 当接口存在枚举、状态值、排序字段、分页上限、公共接口无权限等边界时，应在 Swagger 注释或 DTO 注释中写明，避免前端和外部调用方靠猜。
- 使用项目既有方式生成并核对 `docs/docs.go`、`docs/swagger.json`、`docs/swagger.yaml`。

`docs` 下文件是生成文件，不手工编辑。不要执行会通过 `@latest` 隐式升级 Swagger 工具的命令，除非用户明确同意。

启动程序会尝试生成 Swagger，并向 Apifox 发起外部同步请求。因此未经明确授权，不执行 `go run .`、启动服务或其他会触发 Apifox 同步的操作。

## 错误与日志

- 所有 `error` 必须及时处理或明确向上返回，不得忽略、吞掉或用空分支处理。
- 不使用 `panic` 处理普通业务错误。
- 使用项目现有日志方式，不保留 `fmt.Println` 等临时调试输出。
- 日志不得记录密码、Token、完整 Authorization Header、密钥或敏感连接信息。
- 日志应包含定位问题所需的业务上下文，但避免输出完整请求体中的敏感字段。

## Go 编码

- 修改的 Go 文件必须使用 `gofmt`，但不要格式化无关文件。
- import、命名、函数风格和注释保持现有代码一致。
- 函数职责清晰，避免重复代码、超长函数和无意义抽象。
- 不新增依赖或升级 Go 模块，除非需求明确授权。
- 不修改公共模型、统一响应、配置加载、GORM 初始化或公共工具行为，除非需求明确要求并已评估调用方。

## 验证

在 `D:\vben\hive-admin-go` 中按修改范围执行：

```bash
gofmt -w <本次修改的.go文件>
go test ./...
go vet ./...
go build ./...
```

当前阶段默认不新增 `_test.go` 文件。`go test ./...` 可作为已有测试和编译检查命令使用；只有用户明确要求、已有测试文件需要同步维护，或改动高风险且已先说明必要性时，才新增或修改正式测试文件。没有实际测试用例时不得声称完整测试已通过。

不要为了验证启动服务、连接生产数据库、执行迁移或同步 Apifox。命令因环境或存量问题失败时，应说明失败位置及其与当前修改的关系。

## 完成检查

- 是否保持 Router、Controller、Service、Model 分层。
- 是否复用了已有实现并保持统一响应。
- 是否正确处理参数、错误、事务、权限和敏感信息。
- 每条受保护路由是否硬编码独占且符合规范的权限码，公共路由是否明确未注册权限中间件。
- 路由权限码是否已在交付说明中列出对应的独立按钮节点配置清单，按钮的建议层级、`name`、`title`、`auth_code` 是否符合约定，且迁移 SQL 未自动写入菜单权限数据。
- 是否存在 SQL 注入、N+1、循环查询或无上限查询。
- 是否同步 Swagger、前端 API 和数据库脚本。
- 是否按“业务文档一致性规则”同步 `CONTEXT-MAP.md`、后端业务正文、前端 UI 文档和就近 `AGENTS.md`，或明确核对后无需修改。
- 是否执行了必要验证，是否影响其它模块和接口。

## 交付时的手动配置清单

- 任务完成后，必须列出需要手动配置的数据。
- 按钮权限必须使用三列表格：`标题`、`国际化`、`权限码`。
- 菜单权限必须列出六项：`类型`、`标题`、`名称`、`路径`、`组件`、`图标`。
- 其它需要手动调整的数据逐项使用列表或表格列出；没有其它数据时明确写“无”。

最终代码应保持一致的架构、命名、接口、数据库和错误处理风格，优先稳定性、正确性和可维护性。
